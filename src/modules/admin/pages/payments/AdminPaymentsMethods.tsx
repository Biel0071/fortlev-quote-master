import PaymentDevPlaceholder from "./PaymentDevPlaceholder";
export default function AdminPaymentsMethods() {
  return <PaymentDevPlaceholder title="Métodos de Pagamento" description="Configure PIX, Cartão, Boleto e Split de Pagamento." />;
}
